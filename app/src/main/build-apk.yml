name名称: Build APK构建APK构建APK构建APK

on在: [push]推送]在: [推送]

jobs:作业:
  build:构建:
    runs-on运行于: ubuntu-latest运行环境: ubuntu-latest
    steps:步骤:
      - uses使用: actions/checkout@v3      - 使用: actions/checkout@v3

      - name: Set up JDK 17      - name: 设置 JDK 17
        uses: actions/setup-java@v3
        with:与:
          java-version: '17'java版本: '17'
          distribution: 'temurin'发行: '特穆林'发行:'特穆林'

      - name: Build with Gradle      - 名称: 使用 Gradle 构建-名称:使用 Gradle 构建
        run: ./gradlew build --no-daemon运行: ./gradlew build --no-daemon

      - name: Upload APK      - 名称: 上传APK
        uses: actions/upload-artifact@v3用途: actions/upload-artifact@v3
        with:与:
          name: apk名称: apk名称: apk名称: apk名称: apk名称: apk名称: apk名称: apk
          path: app/build/outputs/apk/**/*.apk路径: 路径: app/build/outputs/apk/**/*.apk路径: app/build/outputs/apk/**/*.apk
